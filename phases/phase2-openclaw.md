# Phase 2 — OpenClaw Installation & Configuration

> ⏳ Pending completion of Phase 1

## Goal
Install OpenClaw and connect it to the local Ollama instance.

## Prerequisites
- [ ] Phase 1 complete (Ollama running, model pulled)
- [ ] Node.js 22.x installed (already present on this system)

## Notes (to be filled during execution)

- OpenClaw must be installed via its **official installer** from openclaw.ai — not ad-hoc npm packages
- Endpoint format: `http://localhost:11434/v1` (requires `/v1` suffix)
- API format setting: `openai-responses`
